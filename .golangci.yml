version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - sqlclosecheck
    - bodyclose
    - misspell
    - revive

formatters:
  enable:
    - gofmt
    - goimports
